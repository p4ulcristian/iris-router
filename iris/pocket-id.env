# Pocket ID config — live file at /var/lib/pocket-id/.env
# Reference copy only, do NOT commit secrets to git

APP_URL=https://auth.irisdoes.work
TRUST_PROXY=true
PUID=1000
PGID=1000
DATA_DIR=/var/lib/pocket-id

# ENCRYPTION_KEY=<generate with: openssl rand -base64 32>
